#!ipxe

:menu
menu Options de démarrage
item --key r reinstall Réinstaller l'image
item --key c continue Continuer le démarrage avec le disque local
item timeout 5000 fallback Continuer automatiquement dans 5 secondes

choose --default continue --timeout 5000 selected && goto ${selected}

:reinstall
chain http://10.10.62.210/install.ipxe

:continue
sanboot --no-describe --drive 0x80 || goto menu

:fallback
goto continue
